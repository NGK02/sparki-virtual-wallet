/** @type {import('tailwindcss').Config} */

module.exports = {
    darkMode: 'class',
    content: ["./views/**/*.cshtml"],
    theme: {
        //colors: {
        //    'text': "hex(var(--color-text) / <alpha-value>)",
        //    'background': "hex(var(--color-background) / <alpha-value>)",
        //    'primary': "hex(var(--color-primary) / <alpha-value>)",
        //    'secondary': "hex(var(--color-secondary) / <alpha-value>)",
        //    'accent': "hex(var(--color-accent) / <alpha-value>)",
        //},

        extend: {
        },
  },
  plugins: [],
}

